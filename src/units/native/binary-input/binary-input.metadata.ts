import {
    BACnetPropIds,
    BACnetObjTypes,
    BACnetPropTypes,
} from '../../../core/enums';

import {
    IBACnetObject,
} from '../../../core/interfaces';

export const BinaryInputMetadata: IBACnetObject = {
    type: BACnetObjTypes.BinaryInput,
    instance: 0,
    props: [
    ]
};
